[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[role_definition]
g = _, _

[permission_definition]
p = admin, task, create
p = user, task, read
p = user, (task owned by user), update
p = admin, task, update
p = user, (task owned by user and not done), delete
p = admin, task, delete